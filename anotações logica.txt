Anotações de tags. 

SCRIPt abre e fecha uma tag java script
BR pula a linha em HTML 
document.write é para escrita
alert exibe um alerta na tela 
var declarar variaveis
Math.round é usado para arredondar = Math.round (media)
funções = como abrir uma função
	function pulaLinha() {
		document.write("<br>");
		}
nomes de funções sempre devem ser um verbo.

funções com paramentro.

function mostra (idade) {	
		
			document.write(idade)
			pulaLinha ();
		
		}

<p>. exibe um paragrafo.		


var idadeCarol = 25;
		var idadeRafa = 31;
		var idadeFred = 5;
		var media = (idadeCarol + idadeRafa + idadeFred)/3;
		
		document.write ("a media das idades é " + Math.round (media));
		
		var nome = "Caroline"
		
		document.write ("<br>A idade de " + nome + " é " + idadeCarol);
